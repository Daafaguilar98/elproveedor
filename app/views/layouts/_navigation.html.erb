<header class="indexheader headerall"> <!-- Cabezera del Homepage -->
  <figure>
    <h2>EL PROVEEDOR</h2>
  </figure>
  <nav class="navigation">
    <ul>
      <li class="ingresar">
      	<% unless user_signed_in? %>
        	<%= link_to 'Ingresar Usuario', user_session_path %>
      	<% else %>
      		<%= link_to('Salir', destroy_user_session_path, :method => :delete) %>
      	<% end %>
      </li>
    </ul>
  </nav>
</header>