<%= form_for :user, :url => session_path(:user), :html => {:class => 'new_user'} do |f| %>

  <div class="field">
    <%= f.number_field :numeroidentificacion, autofocus: true, placeholder: "Numero De Identificacion",required: ""  %>
  </div>
  <div class="field">
    <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña",required: "" %>
  </div>

 <!-- <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end -%> -->

  <div class="actions">
    <%= f.submit "Entrar", class: ' entrar btn' %>
  </div>
<% end %>

<!-- <%= render "devise/shared/links" %> -->

